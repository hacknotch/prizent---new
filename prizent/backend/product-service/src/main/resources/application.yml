server:
  port: 8084

spring:
  application:
    name: product-service
  security:
    user:
      name: disabled  
      password: disabled
  datasource:
    url: jdbc:mysql://localhost:3306/product_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: preetam@123P

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB
      file-size-threshold: 2KB

jwt:
  secret: ${JWT_SECRET:mySecretKeyForDevelopmentOnlyChangeInProduction}
  expiration: 14400000 # 4 hours in milliseconds

# Admin Service Configuration (for inter-service communication)
admin:
  service:
    url: http://localhost:8082/api/admin

logging:
  level:
    com.elowen.product: DEBUG